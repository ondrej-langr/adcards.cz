<!-- start @app/partials/pages/cart/shipping/zasilkovna-select.twig -->
<template x-if='shippingMethod === "zasilkovna"'>
    <div>
        <button type="button" @click="showZasilkovnaSelect()"
                x-text='shippingMetadata ? shippingMetadata.name : "{{ 'Vybrat místo'|t }}"'
        ></button>
        <input class='w-100 '
               style='max-height: 0px; min-height: 0px;padding: 0;margin: 0;border:0;opacity: 0; top: -10px;position: relative;'
               required
               name='shippingMetadata'
               :value='shippingMetadata? JSON.stringify(shippingMetadata) : undefined' />
        <small style='margin-top: -1rem' class="field-error d-block"
               x-text='state?.form?.errors.shippingMetadata'></small>
    </div>
</template>
<!-- end @app/partials/pages/cart/shipping/zasilkovna-select.twig -->

